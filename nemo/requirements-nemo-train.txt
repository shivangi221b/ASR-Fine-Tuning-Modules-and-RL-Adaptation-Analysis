# NeMo 2.x requires Python 3.10+ (use python3.11 -m venv .venv).
# NeMo ASR preprocessing uses np.sctypes (removed in NumPy 2.0); keep NumPy 1.x until NeMo ships a fix.
#
# PyTorch: do NOT use an unqualified `torch` from PyPI on GCP — it often resolves to cu128 and
# fails at import / NeMo init with: "NVIDIA driver ... too old (found version 12080)" when the
# VM driver is older than that CUDA userland. Pin cu121 wheels (works on typical T4 drivers).
--extra-index-url https://download.pytorch.org/whl/cu121
numpy>=1.22,<2.0
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
# HuggingFace datasets 3.x dropped dataset scripts; AfriSpeech still ships afrispeech-200.py
datasets>=2.14.0,<3.0.0
soundfile
librosa
jiwer
pandas
omegaconf
lightning>=2.0.0
nemo_toolkit[asr]>=2.0.0
google-generativeai>=0.8.0
psutil>=5.9.0
