# NeMo 2.x requires Python 3.10+ (use python3.11 -m venv .venv).
# Core deps first so `pip install -r ...` still leaves numpy/torch if NeMo fails partway.
# NeMo ASR preprocessing uses np.sctypes (removed in NumPy 2.0); keep NumPy 1.x until NeMo ships a fix.
numpy>=1.22,<2.0
torch
# HuggingFace datasets 3.x dropped dataset scripts; AfriSpeech still ships afrispeech-200.py
datasets>=2.14.0,<3.0.0
soundfile
librosa
jiwer
pandas
omegaconf
lightning>=2.0.0
nemo_toolkit[asr]>=2.0.0
google-generativeai>=0.8.0
psutil>=5.9.0
